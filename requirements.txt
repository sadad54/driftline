# Core
pandas==2.2.2
numpy==1.26.4
pyarrow>=15.0.0
duckdb>=1.0.0

# ML
scikit-learn==1.4.2
xgboost==2.0.3
torch==2.10.0
torch-geometric>=2.5.0
# pyg-lib needs the PyG wheel index, not plain pip -- install separately:
#   pip install pyg-lib -f https://data.pyg.org/whl/torch-2.10.0+cpu.html
# (required for NeighborLoader's accelerated sampling; without it PyG falls back to an
# unsupported pure-Python sampler that raises ImportError)

# Streaming (installed separately on the VM, needs Java 11 -- see streaming/download_jars.sh)
# apache-flink

# Serving
fastapi==0.111.0
uvicorn[standard]>=0.29.0
onnx>=1.16.0
onnxruntime>=1.17.0
skl2onnx>=1.16.0

# Data infra (installed now; used once VM/Docker is up)
kafka-python>=2.0.2
redis>=5.0.0
feast>=0.40.0

# Experiment tracking / eval
mlflow>=2.13.0
evidently>=0.4.30

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
testcontainers>=4.4.0
httpx>=0.27.0

# Utilities
kaggle
python-dotenv>=1.0.0
