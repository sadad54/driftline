# Lean dependency set for the serving container -- deliberately NOT the full requirements.txt
# (no torch/torch-geometric/pyflink/kaggle -- the scorer only needs to run the already-exported
# ONNX model and look up Feast online features, not retrain anything).
fastapi==0.111.0
uvicorn[standard]>=0.29.0
onnxruntime>=1.17.0
feast>=0.40.0
redis>=5.0.0
pandas==2.2.2
numpy==1.26.4
pydantic>=2.0.0
prometheus-client>=0.20.0
